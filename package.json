{
  "scripts": {
    "start": "node start-stack.js",
    "install:all": "npm install --prefix Betception-Backend && npm install --prefix Betception-Frontend",
    "build": "npm run build --prefix Betception-Backend && npm run build --prefix Betception-Frontend",
    "build:backend": "npm run build --prefix Betception-Backend",
    "build:frontend": "npm run build --prefix Betception-Frontend"
  },
  "devDependencies": {
    "@types/jasmine": "^5.1.9",
    "jasmine-core": "^5.12.0",
    "karma": "^6.4.4",
    "karma-chrome-launcher": "^3.2.0",
    "karma-jasmine": "^5.1.0",
    "karma-jasmine-html-reporter": "^2.1.0"
  }
}
